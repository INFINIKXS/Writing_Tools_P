how does the formatter work and why does it require AI, since the user provided an input, if doi is present, simply fetch the information from either pubmed or crossref, and use the metadata to reconstruct the reference in the desired format. also if the initial input was wrong in any way you alert the user by pointing out what is wrong and what was corrected based on information from the doi. if doi is not present, simply use the user's input as sole source and reconstruct. also as the result is coming out it should be displayed on the ui, dont wait for everything to finish before display.

NEW
Move FORMATER FEATURE TO LIBRARY
